{
  "name": "vrsi-wallboard",
  "version": "0.15.1",
  "private": true,
  "description": "VRSI WallBoard — local standalone job board + calendar kiosk",
  "scripts": {
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix shared && npm run build --prefix client && npm run build --prefix server",
    "start": "npm run start --prefix server",
    "e2e:tour": "playwright test --config e2e/playwright.config.ts",
    "e2e:report": "playwright show-report e2e/artifacts/report",
    "e2e:video": "node e2e/export-videos.cjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
