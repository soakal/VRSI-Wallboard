node_modules/
dist/
*.db
*.db-wal
*.db-shm
.env
.env.production
client/.env.production
logs/
backups/
*.tsbuildinfo
*.migrated
server/data/
VRSI WallBoard/
client/.tsbuild-node/
client/vite.config.js
client/vite.config.d.ts
shared/src/**/*.js
shared/src/**/*.d.ts
e2e/artifacts/
e2e/.demo-data/
# Release artifacts — Package-Release.ps1 writes them to releases/ (gitignored).
# The root-level patterns below catch any artifacts that got built before this
# folder structure was in place (keep until the root is clear).
releases/
VRSI-WallBoard-v*.zip
*.zip.sha256
*.sha256
*.lnk
.claude/
